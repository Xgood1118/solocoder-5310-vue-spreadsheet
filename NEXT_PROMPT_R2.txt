公式栏那个还是不行。我手动在 fx 框里输入 `=AVERAGE(B2:C2)` 按回车，单元格显示的就是原文 `AVERAGE(B2:C2)`，类型还变成字符串了，F12 看 localStorage 里存的 value 也是 `AVERAGE(B2:C2)`，连 `=` 都没有。我又试了 `=SUM(B2:C2)` 一样的问题。初始化时通过 setCell 写进去的 D 列那些 `=SUM(...)` 都正常能求值，就是手动在公式栏敲的进不去引擎。检查一下输入到 setCell 这一段到底在哪儿把 `=` 给吞了。修一下。
