样例数据里的 SUM 公式能正常算（D2=2700，B5=3500），多 sheet 切换、新增、删除也都能用。但我手动在公式栏里敲 `=AVERAGE(B2:C2)` 进去，单元格变成"字符串"类型显示原文，公式根本没被求值——只对初始化时通过 setCell 写进去的公式才会被引擎解析。我看 Spreadsheet.vue 第 247 行 `sheetData[sheet.id] = sheet.data || {}` 写错了，应该从 `data_${sheet.id}` 读取，否则一刷新数据全没（localStorage 里数据键名是 `data_xxx`）。还有 PROMPT 列的 5 状态工作流（草稿/已审阅/已发布/已锁定/已归档）、5 角色权限、免费版 5 sheet 上限（实测能加到 6 个以上没拦截）、1000 行 100 列上限、24h 公式缓存过期、多 tab BroadcastChannel 同步——这些在源码里都没找到实现。检查修复一下。
